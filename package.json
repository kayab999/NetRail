{
  "name": "netrail",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev -- --bin netrail",
    "build": "tauri build -- --bin netrail"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}